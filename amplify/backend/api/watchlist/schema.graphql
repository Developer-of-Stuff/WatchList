type Media @model @auth(rules: [{ allow: private, provider: userPools }]) {
  id: ID!
  name: String!
  medium: Medium!
  creatorId: ID!
  creatorName: String!
  watched: Boolean!
}

enum Medium {
  MOVIE
  SHOW
}
